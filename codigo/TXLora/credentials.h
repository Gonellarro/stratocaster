#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Copia este fichero como "credentials.h" (mismo directorio que TXLora.ino)
// y rellena tus datos reales. "credentials.h" NUNCA debe subirse a git
// (añádelo a .gitignore).

#define WIFI_SSID     "NovaWifi"
#define WIFI_PASSWORD "Marti07Emma21Maria30"

#endif
